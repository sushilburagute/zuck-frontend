import React from "react";

function Header(props) {
    return (
        <header className="App-header">
            <h1 className="font-semibold">Zuck</h1>
        </header>
    );
}

export default Header;
